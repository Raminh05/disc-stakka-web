# Pinned. The container build asserts that the `hid` module is still the libusb
# backend, so bumping hidapi means rebuilding the image and watching that check
# rather than trusting the resolver.
Flask==3.1.3
Pillow==12.3.0
requests==2.34.2
hidapi==0.15.0
